policies:
- name: delete-unused-cosmosdb
  resource: azure.cosmosdb
  description: |
    Delete Cosmos DB accounts that have had no requests in the last 72 hours
  filters:
  - type: metric
    metric: TotalRequests
    op: eq
    threshold: 0
    timeframe: 72
  actions:
  - type: delete
